# PDF documents (stored via Git LFS)
*.pdf filter=lfs diff=lfs merge=lfs -text

# CSV datasets (raw, non-code data)
*.csv linguist-vendored
*.csv -diff -merge

# Checksum files
SHA256SUMS linguist-vendored

# Markdown documentation
*.md text eol=lf
